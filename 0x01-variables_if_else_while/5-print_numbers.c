#include <stdio.h>

/**
 * main - print all single number of base
 * Return: Always 0 success
 */
int main(void)
{
	int n;
	
	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
